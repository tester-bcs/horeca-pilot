# luck-core — REFERENCE-форк

Происхождение: ~/workspace/luck-repo/repo/rust (канонический Rust-крейт Luck,
зеркало Python-ветки ~/projects/luck), скопирован 2026-08-11 как reference.

НАЗНАЧЕНИЕ: справочная копия для сверки расхождений синтаксисов.
НЕ является рабочим движком пайпа (не понимает ai-agent-синтаксис: VERIFY-узел,
BRANCH/MERGE kinds, plan.json — там 9 kinds, VERIFY-слот, REJECT-Mark).
Рабочий прототип — ../luck-pilot (форк ai-agent-порта).

Нативный репозиторий НЕ модифицируется — эта копия живёт только здесь.
Если нативный репозиторий обновится — reference можно пере-скопировать
(сверять по дате копирования).
